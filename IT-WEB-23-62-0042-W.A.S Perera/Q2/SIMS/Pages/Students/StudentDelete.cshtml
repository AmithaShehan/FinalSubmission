@page "/student/delete"
@model SIMS.Pages.Students.StudentDelete
@{
    ViewData["Title"] = "Delete Student";
}

<div class="container">
    <div class="row">
        <div class="col col-md-6 offset-md-3">
            <div class="card">
                <div class="card-body">
                    <h3 class="text-primary">Are you sure you want to delete this entry?</h3>
                    <h5 class="card-title">@Model.StudentData.StudentName</h5>
                    <h6 class="card-title">City - @Model.StudentData.City</h6>
                    <div class="card-body">
                        <form method="post">
                        <button type="submit" class="btn btn-danger">Delete</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>